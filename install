#!/usr/bin/env bash
set -eu -o pipefail
shopt -s extglob

cd $(dirname "${BASH_SOURCE[0]}")

for file in !(install|README.md|chisel); do
  target="${HOME}/.${file}"
  ln -s "$(pwd)/$file" "$target" &>/dev/null && echo "linking $file"
done
